Compiled with C++ 17.

All solutions assume an input file (named "input", with no extension) in the current working directory of the solution's binary. The input file should have the original unix line endings.
